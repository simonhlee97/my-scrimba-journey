import React from 'react'

export default function Context() {
	return (
		<div>
			<h2>Context API</h2>
			<p>
				The Context API solves the problem of "prop-drilling". From a single "Provider"
				component, data can be passed down to any "Consumer" component that wants to use
				that data.
			</p>
		</div>
	)
}
