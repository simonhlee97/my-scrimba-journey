import React from 'react'

export default function Notes() {
    return (
        <div>
            <p>Netflix Clone - made with:</p>
            <p>Compound Components - a design pattern in which components are collated together to create a complete experience.</p>
            <p>Styled Components (CSS in JS)</p>
            <p>Firebase</p>
            <p></p>
        </div>
    )
}
